[package]
name = "room-api"
version = "0.2.0"
edition = "2024"
publish = false

[dependencies]
room-light = { path = "../light" }

# Trait version implemented by embassy-net 0.9.1; shared across both board-specific stacks.
embedded-io-async = "0.7"
httparse = { version = "1.10", default-features = false }
serde = { version = "1", default-features = false }
serde-json-core = "0.6"
heapless = "0.9"
